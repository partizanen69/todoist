import styled from 'styled-components';

export default styled.div`
	.wrapper {
		display: flex;
		justify-content: flex-start;
	}

	.form {
		display: flex;
		justify-content: flex-start;

		& form {
			width: 210px;
		}
	}
`;
